system hostname nv4-A2-v8-{{ ("%03d"|format(train_id)) }}

system admin password `seI2rPeFpS.JL6Ad3p9RV/kTTrgVAA`

{% include 'vlans.j2' %}

spanning-tree enable
lldp run
no ip dhcp-option82 enable

{% include 'dhcp_groups/fis_group.j2' %}
{% include 'dhcp_groups/video_group.j2' %}
{% include 'dhcp_groups/afz_group.j2' %}
{% include 'dhcp_groups/sprechstelle_group.j2' %}
no ip dhcp-snooping enable
ip dhcp-server enable

interface e0-0
  description "Switch A3"
  switchport mode trunk prune allow 1-1000
  lldp mgmt-address vlan100
  spanning-tree edge off
  enable

interface e0-1
  description "Switch C3"
  switchport mode trunk prune allow 1-1000
  lldp mgmt-address vlan100
  spanning-tree edge off
  enable

interface e0-2
  no enable

interface e0-3
  no enable

interface e0-4
  description "AP A2"
  switchport mode trunk prune allow 100,10,20,30,31,131,150,1
  lldp mgmt-address vlan100
  poe priority critical
  spanning-tree edge on
  enable

interface e0-5
  description "Reserved"
  no enable

interface e1-0
  description "Interior Camera A8"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.15
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.143
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-1
  description "Interior Camera A4"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.16
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.144
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-2
  description "Interior Camera A3"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.17
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.145
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-3
  description "Interior Camera A6"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.18
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.146
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-4
  description "Interior Camera A10"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.19
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.147
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-5
  description "AFZ A3"
  switchport mode access vlan 8
  dhcp-server client-group afz
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ train_id // 2 }}.7
  {%- else %}
  dhcp-server client-address 172.20.{{ (train_id - 1) // 2 }}.135
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-6
  description "AFZ A4"
  switchport mode access vlan 8
  dhcp-server client-group afz
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ train_id // 2 }}.8
  {%- else %}
  dhcp-server client-address 172.20.{{ (train_id - 1) // 2 }}.136
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-7
  description "NVR A"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.5
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.133
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-8
  description "Sprechstelle A2"
  switchport mode access vlan 9
  dhcp-server client-group sprechstelle
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ 128 + (train_id // 2) }}.6
  {%- else %}
  dhcp-server client-address 172.20.{{ 128 + ((train_id - 1) // 2) }}.134
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-9
  description "Sprechstelle A1R"
  switchport mode access vlan 9
  dhcp-server client-group sprechstelle
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ 128 + (train_id // 2) }}.5
  {%- else %}
  dhcp-server client-address 172.20.{{ 128 + ((train_id - 1) // 2) }}.133
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-10
  no enable

interface e1-11
  no enable

interface e1-12
  description "Funksensor A2"
  switchport mode access vlan 90
  no enable

interface e1-13
  description "Funksensor A4"
  switchport mode access vlan 90
  no enable

interface e1-14
  description "Seitenanzeige A2"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.27
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.155
  {%- endif %}
  spanning-tree edge on
  enable

interface e1-15
  description "Seitenanzeige A4"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.28
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.156
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-0
  description "Bildschirm A8"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.54
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.182
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-1
  description "Bildschirm A6"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.55
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.183
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-2
  description "Bildschirm A2"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.56
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.184
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-3
  description "Bildschirm A4"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.57
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.185
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-4
  no enable

interface e2-5
  description "Audio Amp. A2"
  switchport mode access vlan 9
  dhcp-server client-group sprechstelle
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ 128 + (train_id // 2) }}.22
  {%- else %}
  dhcp-server client-address 172.20.{{ 128 + ((train_id - 1) // 2) }}.150
  {%- endif %}
  spanning-tree edge on
  enable

interface vlan1
  ip address 192.168.1.101 255.255.255.0
  no lldp enable
  enable

interface vlan100
  ip address dhcp
  enable

{% if train_id < 128 -%}
ntp client source address 10.178.{{ train_id }}.1
{%- else -%}
ntp client source address 10.179.{{ train_id - 128 }}.1
{%- endif %}

snmp-server engineid 80000a0593a212121
snmp-server community snmpadmin rw
snmp-server user snmpadmin group snmpadmin sha1 "NomadStayOut!" aes "NomadStayOut!"
snmp-server group-access snmpadmin priv readview snmpadmin writeview snmpadmin
snmp-server view snmpadmin .1 included
snmp-server view snmpadmin .ISO included
snmp-server enable

service cmd-load enable
service ssh-server enable
no service telnet-server enable
no service http-rest-server enable
service https-rest-server enable
service console-server enable
service auto-usb-server enable
service tftp-server enable
