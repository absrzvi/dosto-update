system hostname nv4-B2-v8-{{ ("%03d"|format(train_id)) }}

system admin password `seI2rPeFpS.JL6Ad3p9RV/kTTrgVAA`

{% include 'vlans.j2' %}

spanning-tree enable
lldp run
no ip dhcp-option82 enable

{% include 'dhcp_groups/fis_group.j2' %}
{% include 'dhcp_groups/video_group.j2' %}
{% include 'dhcp_groups/afz_group.j2' %}
{% include 'dhcp_groups/sprechstelle_group.j2' %}
no ip dhcp-snooping enable
ip dhcp-server enable

interface e0-0
  description "Switch B3"
  switchport mode trunk prune allow 1-1000
  lldp mgmt-address vlan100
  spanning-tree edge off
  enable

interface e0-1
  description "Switch F3"
  switchport mode trunk prune allow 1-1000
  lldp mgmt-address vlan100
  spanning-tree edge off
  enable

interface e0-2
  no enable

interface e0-3
  no enable

interface e0-4
  description "AP B2"
  switchport mode trunk prune allow 100,10,20,30,31,131,150,1
  lldp mgmt-address vlan100
  poe priority critical
  spanning-tree edge on
  enable

interface e0-5
  description "Reserved"
  no enable

interface e1-0
  description "Interior Camera B8"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.64
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.192
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-1
  description "Interior Camera B4"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.65
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.193
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-2
  description "Interior Camera B3"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.66
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.194
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-3
  description "Interior Camera B6"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.67
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.195
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-4
  description "Interior Camera B10"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.68
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.196
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-5
  description "AFZ B3"
  switchport mode access vlan 8
  dhcp-server client-group afz
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ train_id // 2 }}.32
  {%- else %}
  dhcp-server client-address 172.20.{{ (train_id - 1) // 2 }}.160
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-6
  description "AFZ B4"
  switchport mode access vlan 8
  dhcp-server client-group afz
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ train_id // 2 }}.33
  {%- else %}
  dhcp-server client-address 172.20.{{ (train_id - 1) // 2 }}.161
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-7
  description "NVR B"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.6
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.134
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-8
  description "Sprechstelle B2"
  switchport mode access vlan 9
  dhcp-server client-group sprechstelle
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ 128 + (train_id // 2) }}.20
  {%- else %}
  dhcp-server client-address 172.20.{{ 128 + ((train_id - 1) // 2) }}.148
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-9
  description "Sprechstelle B1R"
  switchport mode access vlan 9
  dhcp-server client-group sprechstelle
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ 128 + (train_id // 2) }}.19
  {%- else %}
  dhcp-server client-address 172.20.{{ 128 + ((train_id - 1) // 2) }}.147
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-10
  no enable

interface e1-11
  no enable

interface e1-12
  description "Funksensor B2"
  switchport mode access vlan 90
  no enable

interface e1-13
  description "Funksensor B4"
  switchport mode access vlan 90
  no enable

interface e1-14
  description "Seitenanzeige B2"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.47
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.175
  {%- endif %}
  spanning-tree edge on
  enable

interface e1-15
  description "Seitenanzeige B4"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.48
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.176
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-0
  description "Bildschirm B8"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.102
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.230
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-1
  description "Bildschirm B6"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.103
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.231
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-2
  description "Bildschirm B2"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.104
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.232
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-3
  description "Bildschirm B4"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.105
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.233
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-4
  no enable

interface e2-5
  description "Audio Amp. B2"
  switchport mode access vlan 9
  dhcp-server client-group sprechstelle
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ 128 + (train_id // 2) }}.32
  {%- else %}
  dhcp-server client-address 172.20.{{ 128 + ((train_id - 1) // 2) }}.160
  {%- endif %}
  spanning-tree edge on
  enable

interface vlan1
  ip address 192.168.1.110 255.255.255.0
  no lldp enable
  enable

interface vlan100
  ip address dhcp
  enable

{% if train_id < 128 -%}
ntp client source address 10.178.{{ train_id }}.1
{%- else -%}
ntp client source address 10.179.{{ train_id - 128 }}.1
{%- endif %}

snmp-server engineid 80000a0593a212121
snmp-server community snmpadmin rw
snmp-server user snmpadmin group snmpadmin sha1 "NomadStayOut!" aes "NomadStayOut!"
snmp-server group-access snmpadmin priv readview snmpadmin writeview snmpadmin
snmp-server view snmpadmin .1 included
snmp-server view snmpadmin .ISO included
snmp-server enable

service cmd-load enable
service ssh-server enable
no service telnet-server enable
no service http-rest-server enable
service https-rest-server enable
service console-server enable
service auto-usb-server enable
service tftp-server enable
