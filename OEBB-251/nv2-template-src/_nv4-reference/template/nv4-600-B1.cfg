system hostname nv4-B1-v8-{{ ("%03d"|format(train_id)) }}

system admin password `seI2rPeFpS.JL6Ad3p9RV/kTTrgVAA`

{% include 'vlans.j2' %}

spanning-tree enable
lldp run
no ip dhcp-option82 enable

{% include 'dhcp_groups/service_group.j2' %}
{% include 'dhcp_groups/fis_group.j2' %}
{% include 'dhcp_groups/video_group.j2' %}
{% include 'dhcp_groups/afz_group.j2' %}
{% include 'dhcp_groups/sprechstelle_group.j2' %}
no ip dhcp-snooping enable
ip dhcp-server enable

interface e0-0
  description "Switch B3"
  switchport mode trunk prune allow 1-1000
  lldp mgmt-address vlan100
  spanning-tree port-cost 400100
  spanning-tree edge off
  enable

interface e0-1
  description "Switch F1"
  switchport mode trunk prune allow 1-1000
  lldp mgmt-address vlan100
  spanning-tree edge off
  enable

interface e0-2
  description "Frontkupplung B1"
  switchport mode trunk prune allow 5,15
  {%- if train_id < 10 %}
  spanning-tree port-cost {{ (train_id * 2000000) - 1 + 500000 }}
  {%- else %}
  spanning-tree port-cost {{ (train_id * 2000000) - 1 }}
  {%- endif %}
  spanning-tree edge off
  enable

interface e0-3
  no enable

interface e0-4
  description "AP B1"
  switchport mode trunk prune allow 100,10,20,30,31,131,150,1
  lldp mgmt-address vlan100
  poe priority critical
  spanning-tree edge on
  enable

interface e0-5
  description "Service FIS/CCTV"
  switchport mode access vlan 2
  dhcp-server client-group service
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ train_id // 2 }}.125
  {%- else %}
  dhcp-server client-address 172.17.{{ (train_id - 1) // 2 }}.253
  {%- endif %}
  spanning-tree edge on
  enable

interface e1-0
  description "Interior Camera B7"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.59
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.187
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-1
  description "Interior Camera B2"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.60
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.188
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-2
  description "Interior Camera B1"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.61
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.189
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-3
  description "Interior Camera B5"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.62
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.190
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-4
  description "Interior Camera B9"
  switchport mode access vlan 5
  dhcp-server client-group video
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.18.{{ 128 + (train_id // 2) }}.63
  {%- else %}
  dhcp-server client-address 172.18.{{ 128 + ((train_id - 1) // 2) }}.191
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-5
  description "AFZ B1"
  switchport mode access vlan 8
  dhcp-server client-group afz
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ train_id // 2 }}.30
  {%- else %}
  dhcp-server client-address 172.20.{{ (train_id - 1) // 2 }}.158
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-6
  description "AFZ B2"
  switchport mode access vlan 8
  dhcp-server client-group afz
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ train_id // 2 }}.31
  {%- else %}
  dhcp-server client-address 172.20.{{ (train_id - 1) // 2 }}.159
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-7
  no enable

interface e1-8
  description "Sprechstelle B1"
  switchport mode access vlan 9
  dhcp-server client-group sprechstelle
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ 128 + (train_id // 2) }}.19
  {%- else %}
  dhcp-server client-address 172.20.{{ 128 + ((train_id - 1) // 2) }}.147
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-9
  no enable

interface e1-10
  no enable

interface e1-11
  description "ZFR R"
  switchport mode access vlan 2
  dhcp-server client-group service
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ train_id // 2 }}.2 vlan 2
  {%- else %}
  dhcp-server client-address 172.17.{{ (train_id - 1) // 2 }}.130 vlan 2
  {%- endif %}
  poe priority critical
  spanning-tree edge on
  enable

interface e1-12
  description "Funksensor E5"
  switchport mode access vlan 90
  no enable

interface e1-13
  description "Funksensor E6"
  switchport mode access vlan 90
  no enable

interface e1-14
  description "Seitenanzeige B1"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.45
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.173
  {%- endif %}
  spanning-tree edge on
  enable

interface e1-15
  description "Seitenanzeige B3"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.46
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.174
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-0
  description "Bildschirm B7"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.98
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.226
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-1
  description "Bildschirm B5"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.99
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.227
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-2
  description "Bildschirm B1"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.100
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.228
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-3
  description "Bildschirm B3"
  switchport mode access vlan 3
  dhcp-server client-group fis
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.17.{{ 128 + (train_id // 2) }}.101
  {%- else %}
  dhcp-server client-address 172.17.{{ 128 + ((train_id - 1) // 2) }}.229
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-4
  description "ADU B"
  switchport mode access vlan 9
  dhcp-server client-group sprechstelle
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ 128 + (train_id // 2) }}.4
  {%- else %}
  dhcp-server client-address 172.20.{{ 128 + ((train_id - 1) // 2) }}.132
  {%- endif %}
  spanning-tree edge on
  enable

interface e2-5
  description "Audio Amp. B1"
  switchport mode access vlan 9
  dhcp-server client-group sprechstelle
  {%- if train_id % 2 == 0 %}
  dhcp-server client-address 172.20.{{ 128 + (train_id // 2) }}.31
  {%- else %}
  dhcp-server client-address 172.20.{{ 128 + ((train_id - 1) // 2) }}.159
  {%- endif %}
  spanning-tree edge on
  enable

interface vlan1
  ip address 192.168.1.109 255.255.255.0
  no lldp enable
  enable

interface vlan100
  ip address dhcp
  enable

{% if train_id < 128 -%}
ntp client source address 10.178.{{ train_id }}.1
{%- else -%}
ntp client source address 10.179.{{ train_id - 128 }}.1
{%- endif %}

snmp-server engineid 80000a0593a212121
snmp-server community snmpadmin rw
snmp-server user snmpadmin group snmpadmin sha1 "NomadStayOut!" aes "NomadStayOut!"
snmp-server group-access snmpadmin priv readview snmpadmin writeview snmpadmin
snmp-server view snmpadmin .1 included
snmp-server view snmpadmin .ISO included
snmp-server enable

service cmd-load enable
service ssh-server enable
no service telnet-server enable
no service http-rest-server enable
service https-rest-server enable
service console-server enable
service auto-usb-server enable
service tftp-server enable
