node_modules/
train-ip-allocation-commission/OneDrive_2_04-05-2026.zip

# Scratch / working dir — never commit
.tmp/

# Regenerable / build artifacts
__pycache__/
*.pyc

# Script run-output (not source)
scripts/zbx_reconcile_run.log
scripts/zbx_reconcile.jsonl

# Machine-local Claude harness state
.claude/settings.local.json
.claude/worktrees/
.claude/logs/
.claude/orchestrator-session-*.json

# Windows / Office junk
desktop.ini
~$*

# Large binary design docs — kept on disk, too heavy for git history
design freeze/
# untracked SDD working copy at root (v2.1 set is already tracked; leave those)
/ND-DEL-OBB-035-SDD-002-01_v2.2.docx

# Large firmware images — kept on disk, too heavy for git (44MB+)
*.tar.gz
# Slides working dir (heavy PDF/docx build artifacts)
.frontend-slides/

# Obsidian per-vault UI state (machine-local) — but DO track app.json
# (the graph exclusion filters, so the KB graph scope reproduces on any clone)
.obsidian/*
!.obsidian/app.json
