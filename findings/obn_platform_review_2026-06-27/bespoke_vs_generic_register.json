{
  "baseline": "origin/master v2.3.12; bar = GenericReport",
  "loop_vulnerable": [
    "DostoNeuReport (report_dosto_neu.py)"
  ],
  "below_bar": [
    "DostoNeuReport (report_dosto_neu.py)",
    "VIAReport (report_via.py)",
    "ACEReport (report_ace.py)",
    "DaniReport (report_dani.py)",
    "LunaReport (report_luna.py)"
  ],
  "detail": [
    {
      "report_class": "ACEReport (report_ace.py)",
      "loop_safe": "safe",
      "loop_evidence": "The ONLY enqueue in the main while-loop is report_ace.py:111 `queue.appendleft(to_device)`. It lives inside the SW->SW branch (rule #1, lines 99-119) and is reached only AFTER to_device.coach_number is set at line 105 (`to_device.coach_number = from_device.coach_number + 1`) or line 107 (`= from_device.coach_number - 1`). Rules #2/#3/#4 (lines 123/136/149) set coach_number but `continue` WITHOUT enqueuing (leaf devices), and the unmatched branch (line 163) never enqueues. The visited/skip guard at report_ace.py:90 `if to_device is None or to_device.coach_number is not None: continue` prevents re-processing any already-numbered device. Therefore no enqueue of an unnumbered to_device is possible and every device is enqueued at most once -> BFS terminates. This is the same gating as Generic (report_generic.py:95-96 `if to_device and to_device.coach_number:` + skip at line 87), NOT the unconditional appendleft of the known-vulnerable DOSTO report_dosto_neu.py:158.",
      "none_guarded": "partial",
      "none_evidence": "In-loop get_device IS guarded: report_ace.py:85 `to_device = self.get_device(neighbour)` -> guarded at report_ace.py:90 (`to_device is None`), matching Generic. serial is guarded: report_ace.py:46-47 `device.type == \"BOX\" and device.serial and device.serial.startswith(\"box\")`, matching Generic line 72. BUT the riskiest deref is UNGUARDED: report_ace.py:72 `master_switch = self.get_device(master_switch_mac)` followed immediately by report_ace.py:73 `master_switch.coach_number = 0` with NO None check. get_device returns None via report.py:404 `.get(mac, None)` whenever master_ccu.neighbours[0][\"mac\"] (line 71) is not a discovered device -> AttributeError crash on a misconfigured/partially-discovered consist. Generic has no equivalent unguarded pre-loop deref (its master switch is the CCU itself, found by serial match in the device loop). config/firmware SNMP fields are not dereferenced in this method.",
      "unmatched_topology_handling": "Clean log+skip, matching Generic's fixed_consist_algo. On an unexpected/unmatched connection the loop falls through all four rule branches to report_ace.py:163 `self.report_unexpected_connection(from_device, to_device, port)`; to_device gets no coach_number, is not enqueued, and the loop continues. No crash, no silent mis-numbering. One latent correctness quirk (not a safety defect): the rule #2 condition at line 123 `if from_device.type == \"SW\" and port == 9 or port == 10:` binds as `(SW and port==9) or (port==10)` due to Python precedence, so any port-10 neighbour regardless of from_device type takes the AP branch — a mis-numbering risk, but it does not affect loop termination and still assigns a coach number rather than crashing.",
      "vs_generic": "below_bar",
      "severity": "medium"
    },
    {
      "report_class": "CCJPAReport (report_ccjpa_wd2.py)",
      "loop_safe": "safe",
      "loop_evidence": "Main loop (while queue, line 102) has 8 enqueue sites, every one gated. Seed: line 98 queue.append(device) gated by `if device.ccu_interface is not None:` (line 95) with coach_number=0 set line 96. Skip guard line 110 `if to_device is None or to_device.coach_number is not None: continue` (same role as Generic line 87, serves as visited guard). Rule appendlefts at 132/152/169/187/202/219/236 each set to_device.coach_number in their own branch BEFORE the appendleft (e.g. line 130 then 132; line 150 then 152). Bypass appendleft line 125 gated by `if handled:` (line 118). No unconditional appendleft(to_device) with coach_number None survives into a re-enqueue cycle — unlike DOSTO report_dosto_neu.py:158. The one weak path: handle_bypass_relay (lines 55-62) is if/elif with no else, so it can return a truthy to_device with coach_number still None; but line 124 `min(min_coach_number, to_device.coach_number)` runs before the appendleft and would raise TypeError (min(int, None)) — i.e. it crashes, it does NOT loop unboundedly. BFS terminates.",
      "none_guarded": "guarded",
      "none_evidence": "get_device() result None-checked at report_ccjpa_wd2.py:110 `if to_device is None or to_device.coach_number is not None: continue`. serial deref guarded at line 89 `device.serial and device.serial.startswith(\"box\")` (matches Generic line 72). No config/firmware deref in number_coaches(). Riskiest deref is line 124 `min(min_coach_number, to_device.coach_number)` reached via the bypass branch when handle_bypass_relay returns a device whose coach_number was never set (no else in the 55-62 if/elif chain) — this is a latent crash on None, but it is preceded by no guard. Not a None-deref of SNMP fields; it is a None passed to min().",
      "unmatched_topology_handling": "Clean log+skip. After all 8 rules fall through, line 267 calls self.report_unexpected_connection(from_device, to_device, port) with NO appendleft — device is left unnumbered and dropped by normalise_devices(increment) at line 271. Equivalent to Generic's fixed_consist_algo report_unexpected_connection + return None path. Does not crash and does not silently mis-number on a genuinely unmatched connection.",
      "vs_generic": "matches_or_better",
      "severity": "low"
    },
    {
      "report_class": "CCJPAWD1Report (report_ccjpa_wd1.py)",
      "loop_safe": "safe",
      "loop_evidence": "Only BFS re-enqueue is line 73 `queue.appendleft(to_device)`, reached solely inside the SW==SW branch (line 58 `if from_device.type == to_device.type == \"SW\":`) and ALWAYS preceded by lines 67-71 `to_device.coach_number = (from_device.coach_number + 1 if direction == \"north\" else from_device.coach_number - 1)` — a non-None int assignment. Combined with the visited/skip guard at line 55 `if to_device is None or to_device.coach_number is not None: continue`, no device is ever re-enqueued while coach_number is still None and no numbered device is reprocessed. Other queue.append calls (line 44, line 122) are CCU/brain-coach seeding gated by `if device.ccu_interface is not None:` (line 41 / line 119), not unconditional traversal re-enqueues. Bounded by finite device set -> terminates. AP (port 9, line 83) and MEDIA (port 6, line 97) branches number but do NOT re-enqueue (continue at 93/112).",
      "none_guarded": "guarded",
      "none_evidence": "Riskiest deref is the .serial.startswith at lines 36-37 `device.serial and device.serial.startswith(\"box\")` — explicitly None-guarded by the preceding `device.serial` truthiness (line 35), same pattern as Generic line 72. get_device() result is None-checked at line 55 `if to_device is None ... : continue` before any to_device attribute access (mirrors Generic line 87). No config/firmware deref in number_coaches().",
      "unmatched_topology_handling": "Logs and skips cleanly. On an unexpected/unmatched connection the final fall-through at line 115 calls self.report_unexpected_connection(from_device, to_device, port) and the for-neighbour loop simply continues; to_device.coach_number is left None so normalise_devices() (line 127, per the line 125-126 comment) drops it. No crash, no silent mis-numbering — equivalent to Generic's fixed_consist_algo no-match path (report_generic.py line 144 report_unexpected_connection + return None).",
      "vs_generic": "matches_or_better",
      "severity": "none"
    },
    {
      "report_class": "DSBReport (report_dsb.py; OTUReport = DSBReport alias at line 200)",
      "loop_safe": "safe",
      "loop_evidence": "number_coaches itself contains NO appendleft; it delegates per train_type (line 87-90). All three enqueues live in the helpers and are each gated by a freshly-set coach_number on a matched signature: (1) dynamic_consist_algo dd-ICL-0-AP branch — `to_device.coach_number = from_device.coach_number + 1` (line 128) THEN `queue.appendleft(to_device)` (line 131); (2) dynamic_consist_algo common branch, guarded by `if signature in directions:` (line 135) — `to_device.coach_number = from_device.coach_number + coach_inc` (line 137) THEN appendleft (line 140); (3) fixed_consist_algo, guarded by `if signature in directions:` (line 189) — `to_device.coach_number = from_device.coach_number + directions[signature]` (line 190) THEN appendleft (line 192). The outer loop also skips already-numbered/unknown devices: `if to_device is None or to_device.coach_number is not None: continue` (line 84). No unconditional appendleft exists (contrast report_dosto_neu.py:158). Termination guaranteed.",
      "none_guarded": "partial",
      "none_evidence": "get_device() result is None-checked at report_dsb.py:84 (`if to_device is None or to_device.coach_number is not None: continue`). SNMP serial is never dereferenced in number_coaches (BOX is selected by `device.type == \"BOX\"`, line 62, not by serial.startswith); the only serial use is in report.py find_type:396 which is guarded `if device.serial and device.serial.startswith(\"box\")`. RISK: report_dsb.py:121-122 dynamic_consist_algo dd-ICL-0-AP branch — `to_switch = self.find_neighbour(to_device, lambda x: x.get(\"port\") == 1)` can return None (find_neighbour returns next(it, None)), then line 122 `to_switch.neighbours = to_switch.neighbours or list()` dereferences to_switch UNGUARDED -> AttributeError if no port-1 neighbour exists. This is a crash path (not a loop bug) on a cabling/topology edge; Generic has no equivalent unguarded chained deref in its algo.",
      "unmatched_topology_handling": "Clean log+skip, matching Generic's fixed_consist_algo. Both helpers fall through to `self.report_unexpected_connection(from_device, to_device, port)` on an unknown signature (dynamic_consist_algo line 144; fixed_consist_algo line 196) and return WITHOUT enqueuing or assigning a coach_number. No crash, no silent mis-numbering. dynamic_consist_algo additionally guards a wrong-way ring via no explicit coach<1 check (it only increments +1 per ICL hop, never decrements) so it cannot produce negative coaches.",
      "vs_generic": "matches_or_better",
      "severity": "low"
    },
    {
      "report_class": "DaisyCyboxReport (report_daisy_cybox.py)",
      "loop_safe": "safe",
      "loop_evidence": "Single enqueue in the main while-loop is `queue.appendleft(to_device)` at report_daisy_cybox.py:51. It is gated three ways: (1) the skip/visited guard `if to_device is None or to_device.coach_number is not None: continue` at line 40; (2) `to_device.coach_number = 1` set at line 44 BEFORE the enqueue in the same iteration, so any future encounter hits the line-40 skip; (3) `if from_device.type == \"BOX\" and to_device.type == \"AP\":` at line 47, with the enqueue inside that block. No device is ever appended while its coach_number is still None. This is the safe pattern, unlike report_dosto_neu.py:158's unconditional appendleft.",
      "none_guarded": "guarded",
      "none_evidence": "get_device() result is None-checked at report_daisy_cybox.py:40 (`if to_device is None ...: continue`) — same guard as report_generic.py:87. number_coaches() performs no .serial/.config/.firmware deref at all: the CCU is identified by find_type(device)==\"BOX\" (lines 26-27), so the GenericReport serial.startswith risk (report_generic.py:72, itself guarded by `if device.serial and ...`) does not even arise here. No unguarded SNMP-field dereference in the class.",
      "unmatched_topology_handling": "No topology-YAML lookup exists — Daisy is a flat single-coach model (all devices forced to coach_number=1, line 44). On an unexpected connection (neighbour that is not a BOX->AP edge), the device is marked coach 1 but simply NOT enqueued (falls through the line-47 `if`); improperly-detected devices are then dropped by normalise_devices() at line 55. It does NOT crash and does NOT loop. Caveat vs Generic: it does NOT call report_unexpected_connection() (Generic does at report_generic.py:144) — so unexpected edges are silently skipped without a warning log, but there is no mis-numbering into a wrong coach since every coach is 1 by design.",
      "vs_generic": "matches_or_better",
      "severity": "low"
    },
    {
      "report_class": "DaniReport (report_dani.py)",
      "loop_safe": "safe",
      "loop_evidence": "Main BFS loop is lines 213-228: `while queue:` then `from_device = queue.pop()` (line 214). It contains NO append/appendleft — the numbered neighbours at lines 227-228 (`to_device.coach_number = from_device.coach_number` / `device_number`) are NEVER re-enqueued (comment lines 224-226 explicitly: \"These have no unnumbered neighbours, so no need to add them to the queue\"). The loop is purely consumptive, draining a finite pre-built queue exactly once. All enqueues happen BEFORE this loop: `queue.append(self.box1)` (line 142), `queue.extend(coach.switches)` (line 186), `queue.extend(current_coach.switches)` (line 206). The preceding coach-walk loop (194-208) terminates because `coaches.discard(current_coach)` (line 207) strictly shrinks the set, with `for...else -> sys.exit(1)` (lines 199-204) on no-match. Safer than Generic's gated `appendleft` — Dani never re-enqueues at all.",
      "none_guarded": "partial",
      "none_evidence": "get_device() result IS guarded: line 221 `if to_device is None or to_device.coach_number is not None: continue` (verbatim match to Generic line 87). device.serial IS guarded: line 138 `if device.serial and device.serial.startswith(\"box1\")`. BUT device.config (SNMP-derived switch hostname) is dereferenced UNGUARDED at line 88 `self.parser.match(switch.config)`, line 95 `switch.config.endswith(\"A\")`, line 103 `self.parser.match(switch.config)`, and line 111 `dev.config.endswith(...)`. If config is None, re.match raises TypeError and str.endswith raises AttributeError. Riskiest deref: line 88 (`_validate_switch_hostname`) — runs on every SW device with no preceding None guard on config.",
      "unmatched_topology_handling": "Crashes via sys.exit(1) on every unexpected/topology-mismatch case rather than log+skip. Bad/unexpected switch hostname -> logger.critical + sys.exit(1) (lines 89-91). Cannot find coach-1 switch on box1 lan1 -> sys.exit(1) (lines 147-149). Cannot find next coach (too many bypasses/broken cables) -> logger.critical + sys.exit(1) (lines 201-204). This is fail-loud (aborts the whole report run), NOT silent mis-numbering — but it is below Generic's fixed_consist_algo behavior, which calls report_unexpected_connection() and returns None (logs+skips that one connection and keeps going).",
      "vs_generic": "below_bar",
      "severity": "low"
    },
    {
      "report_class": "DostoNeuReport (report_dosto_neu.py)",
      "loop_safe": "vulnerable",
      "loop_evidence": "report_dosto_neu.py:158 `queue.appendleft(to_device)` is UNCONDITIONAL — followed by `continue` at :159. The only guard before it is the entry skip at :61 `if to_device is None or to_device.coach_number is not None: continue`, which screens out already-numbered/undiscovered devices but does NOT gate the enqueue on a SUCCESSFUL numbering. The numbering branches (:65-155) only set to_device.coach_number for matched port/type combos; a discovered-but-unnumbered neighbour that matches no branch (unexpected port/topology) keeps coach_number=None yet is still appendleft'd at :158. With no visited set and line 61 keying only on coach_number, mutually/transitively adjacent unnumbered devices can re-enqueue each other unboundedly. Contrast GenericReport report_generic.py:95-96 `if to_device and to_device.coach_number: queue.appendleft(to_device)` (gated) and TGV's `if to_device and to_device.coach_number:`.",
      "none_guarded": "partial",
      "none_evidence": "get_device() results ARE None-guarded everywhere they are dereferenced: report_dosto_neu.py:61 (`to_device is None`), :207-208 (`if other is not None and other.type == \"SW\"`), :246 (`if ap is None or ap.type != \"AP\"`). The riskiest SNMP-derived deref is switch.config in the fallback at :225/:194 — guarded via `config or \"\"` in ident_from_config (:194 `re.match(r\"...\", config or \"\")`). serial/firmware are not dereferenced in this class (unlike Generic's :72 `if device.serial and device.serial.startswith(...)`), so no unguarded serial deref exists. No unguarded None deref found; classified partial only because the class relies on `find_type`/config rather than performing Generic's explicit serial None-check pattern.",
      "unmatched_topology_handling": "CRASH-free but SILENT MIS-NUMBER + feeds the loop bug. On an unexpected connection (a neighbour whose port/type matches none of the branches at :65-155), DostoNeuReport does NOT call report_unexpected_connection() (grep: 0 occurrences in the file) and does NOT skip — it silently leaves coach_number=None and still appendleft's the device at :158. This is strictly below Generic's fixed_consist_algo, which on a topology-YAML miss (suppress(KeyError) at report_generic.py:129-138) calls report_unexpected_connection() and returns None at :143-145 (log + skip, no enqueue).",
      "vs_generic": "below_bar",
      "severity": "high"
    },
    {
      "report_class": "LunaReport (report_luna.py)",
      "loop_safe": "safe",
      "loop_evidence": "Three enqueues, all gated by the visited-equivalent guard. Guard at report_luna.py:39 `if to_device is None or to_device.coach_number is not None: continue`. Immediately after, report_luna.py:43 `to_device.coach_number = 1` sets the coach number unconditionally BEFORE any enqueue. The three appendleft calls all occur after line 43: report_luna.py:49 `queue.appendleft(to_device)` (BOX->SW branch), report_luna.py:55 (SW->SW branch), report_luna.py:67 (SW->AP branch). Because coach_number is set to 1 at line 43 before every appendleft, any device already enqueued/processed fails the line-39 guard on re-encounter and is skipped. coach_number acts as the visited flag => each device enqueued at most once => BFS terminates. This is the safe pattern (unlike DOSTO report_dosto_neu.py:158 which appendlefts unconditionally with no coach_number gate).",
      "none_guarded": "partial",
      "none_evidence": "get_device() result IS None-checked: report_luna.py:35 `to_device = self.get_device(neighbour)` then report_luna.py:39 `if to_device is None or ...: continue`. SNMP-derived fields (serial/config/firmware) are NEVER dereferenced in LunaReport.number_coaches() — the path reads only device.type and device.device_number, so there is no unguarded serial/config/firmware deref here (contrast Generic report_generic.py:72 which guards `if device.serial and device.serial.startswith(...)`). Marked partial because the AP branch leaves device_number unset (None) on a port-mismatch: report_luna.py:60-67, when from_device.device_number==2 and port is neither 5 nor 6, to_device.device_number is never assigned (stays default None) yet the device is still enqueued at line 67 — a None device_number propagates downstream unflagged.",
      "unmatched_topology_handling": "Silently mis-numbers / skips — worse than Generic. LunaReport uses hardcoded if-branches (BOX->SW line 46, SW->SW line 53, SW->AP line 59) instead of a topology-YAML lookup, and has NO report_unexpected_connection() call anywhere. On an unexpected connection: (a) any type combo matching none of the three branches (e.g. SW->BOX, AP->anything) still gets coach_number=1 forced at report_luna.py:43, then falls through all if-blocks with no continue/appendleft — silently assigned to coach 1 with device_number None and its neighbours never traversed, no log; (b) the SW->AP port-mismatch (device_number==2, port not in {5,6}) at report_luna.py:60-67 enqueues the AP with device_number left None — silent mis-numbering. Neither case logs a warning. Generic's fixed_consist_algo (report_generic.py:143-145) calls report_unexpected_connection() and returns None on no-match; Luna does neither.",
      "vs_generic": "below_bar",
      "severity": "low"
    },
    {
      "report_class": "QueenslandReport (report_queensland.py)",
      "loop_safe": "safe",
      "loop_evidence": "The main BFS loop is report_queensland.py:65-123. The ONLY re-enqueue is `queue.appendleft(to_device)` at line 88, inside `if from_device.type == \"SW\":` (line 76) and `if port in [1, 2, 3, 4]:` (line 78). Before that enqueue, to_device.coach_number is unconditionally set: line 80 `to_device.coach_number = from_device.coach_number - 1` (port in [1,3]) OR line 85 `to_device.coach_number = from_device.coach_number + 1` (port in [2,4]); since port is guaranteed in [1,2,3,4], one always runs, so coach_number is non-None before appendleft, then `continue` (line 89). The visited/skip guard at line 72 `if to_device is None or to_device.coach_number is not None: continue` then prevents any already-numbered device from being processed/re-enqueued. No unconditional appendleft of an unnumbered device exists (unlike the known-vulnerable report_dosto_neu.py:158). The AP (92-95), MEDIA (99-107) and CCU (111-118) branches set coach_number but never enqueue. BFS terminates.",
      "none_guarded": "guarded",
      "none_evidence": "get_device() result is None-checked at report_queensland.py:72 `if to_device is None or to_device.coach_number is not None: continue` before any deref (get_device defined report.py:401-404 returns `.get(mac, None)`). SNMP-derived serial is guarded at lines 29-33 `device.type == \"BOX\" and device.serial and device.serial.startswith(\"box\")` before `.startswith` and before the `re.search` at line 36 — same pattern as Generic line 72. config/firmware are not used in number_coaches. Riskiest deref: line 43 `min([ccu.device_number for ccu in ccus])` — if `ccus` is empty (no box-serial device found) this raises ValueError. This is a setup-time crash, not in the BFS loop and not a get_device None-deref; noted as a non-loop robustness gap.",
      "unmatched_topology_handling": "log+skip cleanly. On a connection matching no branch, lines 120-123 call self.report_unexpected_connection(from_device, to_device, port) (report.py:143, logger.warning) and the device is left unnumbered; normalise_devices (report.py:245-260) then drops any device with coach_number/device_number/type None. No crash, no silent mis-number — equivalent to Generic's fixed_consist_algo report_unexpected_connection + return None path. One log defect: the CCU branch (lines 111-118) sets a valid coach_number but lacks a `continue`, so a successfully-numbered Other-CCU still falls through to report_unexpected_connection at line 123, emitting a spurious 'unexpected connection' warning. Cosmetic/log-only — does not mis-number or crash.",
      "vs_generic": "matches_or_better",
      "severity": "low"
    },
    {
      "report_class": "TGV2020Report (report_tgv2020.py)",
      "loop_safe": "safe",
      "loop_evidence": "report_tgv2020.py:142-143 — the only enqueue in the main while-queue loop is `if to_device and to_device.coach_number:` / `queue.appendleft(to_device)`. It is gated by a successful numbering (coach_number set by fixed_consist_algo this iteration). Additionally report_tgv2020.py:134 `if to_device is None or to_device.coach_number is not None: continue` skips already-numbered devices. This is the SAFE pattern, identical to report_generic.py:95-96 — NOT the DOSTO unconditional appendleft.",
      "none_guarded": "guarded",
      "none_evidence": "get_device() result guarded at report_tgv2020.py:130-134 (`if to_device is None or ...: continue`). serial guarded at report_tgv2020.py:114 (`if device.serial and device.serial.startswith(\"box1\")`). Riskiest deref is report_tgv2020.py:35 `switch3.type`, guarded immediately by the same-line short-circuit `if switch3 is None or switch3.type != \"SW\"`. No config/firmware fields are dereferenced in this file.",
      "unmatched_topology_handling": "Log+skip cleanly. fixed_consist_algo (report_tgv2020.py:149-165) wraps the topology lookup in try/except KeyError (lines 152,156); on no match it calls self.report_unexpected_connection(from_device, to_device, port), self.update_state(\"unexpected connection - check logs\"), and returns None (lines 157-160). Equivalent to Generic's suppress(KeyError)+report_unexpected_connection+return None. Does not crash or silently mis-number. (Separately, pre-loop helpers _find_media_coach/_basic_validations use sys.exit(1) on anomalies — clean termination, not a crash/mis-number.)",
      "vs_generic": "matches_or_better",
      "severity": "none"
    },
    {
      "report_class": "TGVReport (report_tgv.py)",
      "loop_safe": "safe",
      "loop_evidence": "Single enqueue in the main while-queue loop: report_tgv.py:70-71 `if to_device and to_device.coach_number: queue.appendleft(to_device)`. Guarded identically to Generic (report_generic.py:95-96). Re-encounter guard at report_tgv.py:44 `if to_device is None or to_device.coach_number is not None: continue` skips already-numbered devices (visited-equivalent). The BOX branch (lines 53-61) sets to_device.coach_number at line 54 before reaching the gate, so its enqueue is still gated by a coach_number that was set that iteration; the regular branch (line 66 fixed_consist_algo) can return None / no coach_number, which line 70's guard correctly suppresses. No unconditional appendleft of an unnumbered device exists (unlike report_dosto_neu.py:158).",
      "none_guarded": "guarded",
      "none_evidence": "report_tgv.py:44 None-checks get_device() result before any deref: `if to_device is None or to_device.coach_number is not None: continue` — the BOX-branch derefs (lines 54-61) all run after this guard. Serial deref at line 26 is guarded: `if device.serial and device.serial.startswith(\"box\")`, which also protects the `int(device.serial[3:4])` at line 28. The riskiest deref is the BOX branch at line 54 (`to_device.coach_number = from_device.coach_number`), but line 44 guarantees to_device is non-None before it. No SNMP config/firmware fields are dereferenced in this method.",
      "unmatched_topology_handling": "Regular (non-BOX) connections are delegated to the inherited GenericReport.fixed_consist_algo (called at report_tgv.py:66), which does suppress(KeyError) topology lookups (report_generic.py:129-138) and, on no match, calls report_unexpected_connection() and returns None (report_generic.py:143-145) — log + skip cleanly, no crash, no silent mis-number. The TGV-only BOX branch (lines 53-61) is a deliberate RD-8261 edge-case handler that bypasses topology lookup to assign box-connected switches directly; it is not an unmatched-topology path.",
      "vs_generic": "matches_or_better",
      "severity": "none"
    },
    {
      "report_class": "VIAReport (report_via.py)",
      "loop_safe": "safe",
      "loop_evidence": "Main numbering BFS lines 229-271. The only enqueue is `queue.appendleft(to_device)` at report_via.py:271, gated at report_via.py:263 by `if neighbour.get(\"mac\") not in parsed_devices:`. parsed_devices is a visited set: every popped device is added via `parsed_devices.add(from_device.mac)` at report_via.py:246. Once a device's mac is in parsed_devices it can never be re-enqueued, so the set of still-enqueueable macs shrinks monotonically and the queue drains -> terminates. NOTE: enqueue is gated by the visited-set, NOT by to_device.coach_number (coach_number is set unconditionally at line 267 or 270 just before the append), so it does NOT mirror Generic's `if to_device and to_device.coach_number:` (report_generic.py:95-96). It instead matches the prompt's \"equivalent visited/skip guard\" criterion. The earlier coach-1 search loop (lines 173-217) uses parsed_devices + multiple break conditions and contains no queue.append at all.",
      "none_guarded": "unguarded",
      "none_evidence": "Riskiest: report_via.py:249-251 `to_device = self.get_device(neighbour)` then immediately `to_device.type` (line 251) with NO None guard. get_device returns None for an unknown MAC (report.py:404 `return self.device_instances.get(mac, None)`), so a neighbour MAC not in device_instances raises AttributeError. Same defect at report_via.py:179-180 (`neighbour_device = self.get_device(neighbour)` then `.type` at 180, unguarded). SNMP-derived config unguarded at report_via.py:258 (`\"AP04\" in to_device.config`) -> TypeError if config is None. By contrast Generic None-checks at report_generic.py:87 (`if to_device is None or ...: continue`) and guards serial at report_generic.py:72 (`if device.serial and device.serial.startswith(...)`). VIA guards config at line 241 (`from_device.config and \"AP04\" in from_device.config`) and None-checks master_switch at lines 145-147 and port3_neighbour at 163, but leaves the per-neighbour get_device derefs and to_device.config (line 258) unguarded.",
      "unmatched_topology_handling": "No topology-YAML / fixed_consist_algo lookup at all; VIA hardcodes numbering rules (ICL->ICL increments coach at line 267, else same coach at 270). There is no report_unexpected_connection() path. Unexpected/unmatched connections are not detected: a stray neighbour is silently numbered into the same or next coach. Devices left with coach_number None after traversal are swept into coach 0 with incrementing device numbers and a warning (lines 279-286) — graceful catch-all but it SILENTLY MIS-NUMBERS rather than flagging a bad connection. This is below Generic's fixed_consist_algo behaviour, which on no topology match calls report_unexpected_connection() and returns None (report_generic.py:143-145).",
      "vs_generic": "below_bar",
      "severity": "high"
    }
  ]
}